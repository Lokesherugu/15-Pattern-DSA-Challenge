int left = 0;
for (int right = 0; right < nums.length; right++) {
    // Expand the window

    // While condition not valid, shrink from left
    while (/* condition */) {
        left++;
    }

    // Possibly update answer
}
